eureka:
    port: ${server.port}
    client:
        serviceUrl:
            defaultZone: http://10.6.71.51:8761/eureka/
        prefer-same-zone-eureka: false
server:
    port: 8090
spring:
    application:
          name: feignservice
feign.hystrix.enabled: true